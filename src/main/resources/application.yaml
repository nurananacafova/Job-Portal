server:
  servlet:
    context-path: /jobs
spring:
  application:
    name: job-portal

  datasource:
    url: jdbc:postgresql://localhost:5432/jobs
    username: postgres
    password: admin123
#    url: ${DATASOURCE_URL}
#    username: ${DATASOURCE_USERNAME}
#    password: ${DATASOURCE_PASSWORD}
#    url: jdbc:postgresql://postgres:5432/jobs
#    username: postgres
#    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true